/** @format */

function ButtonStyle(button, bgColor, rounded, padding) {
  button.style.backgroundColor = bgColor;
  button.style.borderRadius = rounded;
  button.style.padding = padding;
}

export { ButtonStyle };
